import { Map, Returned } from './types';
export declare const useBreakpoints: <POINTS extends string>({ ref, map, isActive, }: {
    ref?: React.RefObject<HTMLElement | SVGGraphicsElement>;
    map: Map<POINTS>;
    isActive?: boolean;
}) => Returned<POINTS>;
