import { DateRange } from '../../../utils/types/Date';
type IsDateInRangeResult = 'first' | 'last' | boolean;
export declare const isDateInRange: (date: Date, range: DateRange, startOf?: (date: Date) => Date) => IsDateInRangeResult;
export {};
