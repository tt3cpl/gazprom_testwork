import _objectWithoutProperties from "@babel/runtime/helpers/objectWithoutProperties";
const _excluded = ["startIcon", "startDirection", "endIcon", "endDirection", "active"];
import { isNotNil } from '@consta/uikit/__internal__/src/utils/type-guards';
import { forwardRefWithAs } from '@consta/uikit/__internal__/src/utils/types/PropsWithAsAttributes';
import React from 'react';
import { AnimateIconBase } from "../AnimateIconBase";
export const AnimateIconSwitcher = forwardRefWithAs((props, ref) => {
  const {
    startIcon,
    startDirection = 0,
    endIcon,
    endDirection = 0,
    active
  } = props,
        otherProps = _objectWithoutProperties(props, _excluded);

  return React.createElement(AnimateIconBase, Object.assign({}, otherProps, {
    ref: ref,
    activeIndex: active ? 1 : 0,
    icons: [startIcon, endIcon].filter(isNotNil),
    directions: startDirection || endDirection ? [startDirection, endDirection] : undefined
  }));
});
//# sourceMappingURL=AnimateIconSwitcher.js.map