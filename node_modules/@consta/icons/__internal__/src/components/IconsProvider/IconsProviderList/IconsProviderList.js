import React from 'react';
import ReactDOM from 'react-dom';
import { cnIcons } from "../IconsProvider";
import { IconsProviderIcon } from "../IconsProviderIcon";
export const IconsProviderList = ({
  icons,
  container
}) => {
  return ReactDOM.createPortal(React.createElement("div", {
    className: cnIcons()
  }, Object.keys(icons).map(key => React.createElement(IconsProviderIcon, {
    element: icons[key].svg,
    name: key,
    key: key
  }))), container);
};
//# sourceMappingURL=IconsProviderList.js.map