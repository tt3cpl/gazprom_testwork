import React, { forwardRef, useContext } from 'react';
import { AnimateIconBase } from "../../components/AnimateIconBase";
import { AnimateIconBaseContext } from "../../components/AnimateIconBaseProvider";
export const withAnimateBaseHOC = params => forwardRef((props, ref) => {
  const activeIndex = useContext(AnimateIconBaseContext);
  return React.createElement(AnimateIconBase, Object.assign({}, params, props, {
    activeIndex: activeIndex,
    ref: ref
  }));
});
//# sourceMappingURL=withAnimateBaseHOC.js.map