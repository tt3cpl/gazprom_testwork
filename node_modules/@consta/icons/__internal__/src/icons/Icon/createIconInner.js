import "./Icon.css";
import { forwardRefWithAs } from '@consta/uikit/__internal__/src/utils/types/PropsWithAsAttributes';
import React from 'react';
import { cnIcon } from "./helpers";
import { Icon } from "./Icon";
export const createIconInner = (createProps, svgComponent) => {
  const Svg = svgComponent;
  return forwardRefWithAs((props, ref) => {
    return React.createElement(Icon, Object.assign({}, props, {
      color: createProps.color,
      name: createProps.name,
      ref: ref
    }), React.createElement(Svg, {
      className: cnIcon('Svg'),
      size: props.size
    }));
  });
};
//# sourceMappingURL=createIconInner.js.map