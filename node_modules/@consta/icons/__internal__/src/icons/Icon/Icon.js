import _objectWithoutProperties from "@babel/runtime/helpers/objectWithoutProperties";
const _excluded = ["size", "className", "view", "as", "children", "color", "name"];
import "./Icon.css";
import { forwardRefWithAs } from '@consta/uikit/__internal__/src/utils/types/PropsWithAsAttributes';
import React from 'react';
import { cnIcon } from "./helpers";
export const Icon = forwardRefWithAs((props, ref) => {
  const {
    size = 'm',
    className,
    view,
    as = 'span',
    children,
    color = 'mono',
    name
  } = props,
        otherProps = _objectWithoutProperties(props, _excluded);

  const Tag = as;
  return React.createElement(Tag, Object.assign({}, otherProps, {
    className: cnIcon({
      size,
      view,
      color
    }, [name, className]),
    ref: ref
  }), children);
});
//# sourceMappingURL=Icon.js.map