import _slicedToArray from "@babel/runtime/helpers/slicedToArray";
import "./TableSeparatorTitles.css";
import { Text } from '@consta/uikit/Text';
import { useAtom } from '@reatom/npm-react';
import React, { memo } from 'react';
import { cn } from "../../../utils/bem";
var cnTableSeparatorTitles = cn('TableSeparatorTitles');
export var TableSeparatorTitles = memo(function (_ref) {
  var lowHeadersAtom = _ref.lowHeadersAtom;

  var _useAtom = useAtom(lowHeadersAtom),
      _useAtom2 = _slicedToArray(_useAtom, 1),
      lowHeaders = _useAtom2[0];

  if (lowHeaders.findIndex(function (column) {
    return column.title && column.isSeparator;
  }) === -1) {
    return null;
  }

  return React.createElement("div", {
    className: cnTableSeparatorTitles()
  }, lowHeaders.map(function (_ref2, index) {
    var title = _ref2.title,
        isSeparator = _ref2.isSeparator;
    return React.createElement("div", {
      className: cnTableSeparatorTitles('Cell'),
      key: index
    }, isSeparator && title && React.createElement(Text, {
      view: "secondary",
      size: "xs",
      className: cnTableSeparatorTitles('Title')
    }, title));
  }));
}, function () {
  return true;
});
//# sourceMappingURL=TableSeparatorTitles.js.map