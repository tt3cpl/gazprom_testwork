import { getByMap } from "./getByMap";
var sizeMap = {
  m: 's',
  s: 'xs'
};
describe('helper getSizeByMap', function () {
  it('Выборка из объекта верная', function () {
    var result = getByMap(sizeMap, 'm');
    expect(result).toEqual('s');
  });
  it('при указании аргумента 3, нужно вернуть значение этого аргумента', function () {
    var result = getByMap(sizeMap, 'm', 'xs');
    expect(result).toEqual('xs');
  });
});
//# sourceMappingURL=getByMap.test.js.map