export var isEq = function isEq(state, newState) {
  for (var key in newState) {
    if (state[key] !== newState[key]) {
      return false;
    }
  }

  return true;
};
//# sourceMappingURL=isEq.js.map