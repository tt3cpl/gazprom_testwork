import { Action, AtomMut, Rec } from '@reatom/core';
export interface RecordAtom<T extends Rec> extends AtomMut<T> {
    merge: Action<[slice: Partial<T>], T>;
    omit: Action<Array<keyof T>, T>;
    reset: Action<Array<keyof T>, T>;
}
export declare const reatomRecord: <T extends Rec>(initState: T, name?: string) => RecordAtom<T>;
//# sourceMappingURL=reatomRecord.d.ts.map