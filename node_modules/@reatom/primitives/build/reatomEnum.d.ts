import { Action, AtomMut } from '@reatom/core';
export type EnumFormat = 'camelCase' | 'snake_case';
export type EnumAtom<T extends string, Format extends EnumFormat = 'camelCase'> = AtomMut<T> & {
    [Variant in T as Format extends 'camelCase' ? `set${Capitalize<Variant>}` : Format extends 'snake_case' ? `set_${Variant}` : never]: Action<[], Variant>;
} & {
    reset: Action<[], T>;
    enum: {
        [K in T]: K;
    };
};
export type EnumAtomOptions<T extends string, Format extends EnumFormat = 'camelCase'> = {
    name?: string;
    format?: Format;
    initState?: T;
};
export declare const reatomEnum: <const T extends string, Format extends "camelCase" | "snake_case" = "camelCase">(variants: ReadonlyArray<T>, options?: string | EnumAtomOptions<T, Format>) => EnumAtom<T, Format>;
//# sourceMappingURL=reatomEnum.d.ts.map